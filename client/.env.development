VITE_DEV_PORT=5173
# Port of the Express API when running locally.
VITE_DEV_SERVER_PORT=5501

# Leave blank to use the Vite dev proxy (/api -> http://127.0.0.1:5501).
# Set to a full origin (e.g., http://127.0.0.1:5501/api) when bypassing the proxy.
# VITE_API_BASE=http://127.0.0.1:5501/api
